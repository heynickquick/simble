PORT=4000

# MongoDB — matches the mongodb service in docker-compose.yml
MONGODB_URI=mongodb://mongo:27017/campaigns

# JWT — generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_LONG_RANDOM_HEX_STRING

# textbee gateway
TEXTBEE_API_URL=https://sms.yourdomain.com
TEXTBEE_API_KEY=your_textbee_api_key_here

# Set to "true" to simulate SMS sends (no real delivery).
# Useful for end-to-end testing without Firebase/phone setup.
TEXTBEE_MOCK=false

# Webhook to call for simulated delivery reports (campaign-manager /api/webhooks/textbee)
SIMBLE_WEBHOOK_URL=https://simble.yourdomain.com/api/webhooks/textbee

# Default device for new clients (admin can override per-client)
TEXTBEE_DEFAULT_DEVICE_ID=your_default_device_id

# Rate limit: max requests per IP per minute
RATE_LIMIT_PER_MIN=120
